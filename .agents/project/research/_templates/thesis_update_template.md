---
description: Template for thesis-quality documentation updates after each experiment trial.
---

# Thesis Update — Trial <TRIAL_ID>

**Date:** <YYYY-MM-DD>  
**Module:** <Temporal Encoder / Morphology Encoder / Fusion / etc.>  
**Updated by:** @thesis-doc  
**Appended to:** `docs/<module>/thesis_notes.md`  

---

## Methodology Note

> <If a new technique or architecture was introduced in this trial, describe it here in academic prose, as it would appear in the Methodology section of the thesis.>
>
> Example:
> "In Trial 4, we introduce the Squeeze-and-Excitation (SE) channel attention mechanism (Hu et al., 2018) to the ResNet-1D backbone. The SE block recalibrates channel-wise feature responses by explicitly modelling interdependencies between channels, allowing the model to focus on the most informative ECG leads."

---

## Experimental Setup

> <Describe the experimental conditions for this trial in thesis-ready language.>
>
> Example:
> "The model was trained on the full PTB-XL dataset (21,837 12-lead ECG records at 100 Hz) using a stratified 10-fold cross-validation split (Fold 1). Training employed the Asymmetric Loss function (Ridnik et al., 2021) with γ+ = 0 and γ- = 4, optimized using AdamW (lr = 3×10⁻⁴, weight_decay = 1×10⁻⁴). The learning rate was scheduled using ReduceLROnPlateau (patience = 5, factor = 0.5) with early stopping (patience = 10)."

---

## Results

> <Present results in thesis-quality language. Use exact metric values. Reference the comparison table.>

**Trial <TRIAL_ID> — <Architecture> + <Strategy>**

| Metric | Trial <TRIAL_ID> | Previous Best | Baseline (BiLSTM) |
|---|---|---|---|
| ROC-AUC (macro) | <value> | <value> | <value> |
| Macro F1 | <value> | <value> | <value> |
| Subset Accuracy | <value> | <value> | <value> |
| Hamming Loss | <value> | <value> | <value> |

> <Narrative of results, e.g.: "The ECGTransformer with MAE pretraining achieved a test ROC-AUC of 0.891 (±0.003), representing a 3.4 percentage point improvement over the previous best configuration (BiLSTM + MAE, 0.857). This improvement is attributed to...">

---

## Analysis & Discussion

> <Analytical prose: WHY did the model perform as it did? What do the metrics tell us? Be specific and mechanistic.>
>
> Consider:
> - Which classes benefited most from this configuration?
> - What does the gap between train and val loss suggest?
> - How does this compare to published results on PTB-XL?
> - What does this tell us about the pretraining strategy's effectiveness?

---

## Barriers & Limitations

> <Document barriers encountered in this trial and their implications for the research.>
>
> Example:
> "Despite improvements in overall ROC-AUC, the model continued to struggle with the HYP (Hypertrophy) class, achieving a per-class F1 of only 0.41. This is consistent with the known label imbalance in PTB-XL, where HYP accounts for only 8.3% of training records. The class imbalance barrier was classified as MEDIUM severity. Asymmetric Loss partially mitigated this effect but did not fully resolve the imbalance."

---

## Shortcomings

> <Honest academic prose about limitations. This is a genuine contribution — not a weakness to hide.>
>
> Example:
> "The current temporal encoder architecture processes each lead independently before aggregation, which may miss inter-lead spatial relationships that are clinically relevant (e.g., axis deviation patterns visible only through lead comparison). A spatial attention mechanism across leads represents a natural extension of this work."

---

## Future Work

> <Research directions identified from this trial. What should be tried that is beyond the current scope?>

- [ ] <Future direction 1>
- [ ] <Future direction 2>
- [ ] <Future direction 3>

---

*Generated by @thesis-doc using the `thesis-writer` skill.*
